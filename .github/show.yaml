name: Show Repository Files

on:
  workflow_dispatch:

jobs:
  list-files:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Show files in repository root
        run: ls -la

      - name: Show files recursively
        run: find . -maxdepth 3 -type f
